#new_question_selector.hidden
  select a new question baby
  %ul
    %li= link_to_remote 'Pick one (radio buttons)', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :pickoneradio), :method => :get
    %li= link_to_remote 'Pick one (drop down)', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :pickonedropdown), :method => :get
    %li= link_to_remote 'Pick any', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :pickany), :method => :get
    %li= link_to_remote 'Simple one-line string', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :string), :method => :get
    %li= link_to_remote 'Multiple one-line strings', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :stringmultiple), :method => :get
    %li= link_to_remote 'Free form text area', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :textarea), :method => :get
    %li= link_to_remote 'Label', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :label), :method => :get
    %li= link_to_remote 'Number: Integer', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :number), :method => :get
    %li= link_to_remote 'Number: Decimal', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :float), :method => :get
    %li= link_to_remote 'Date & Time', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :datetime), :method => :get
    %li= link_to_remote 'Date', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :date), :method => :get
    %li= link_to_remote 'Time', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :time), :method => :get
    %li= link_to_remote 'Slider', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :slider), :method => :get
    %li= link_to_remote 'Grid', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :grid), :method => :get
    %li= link_to_remote 'Rank', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :rank), :method => :get
    %li= link_to_remote 'Repeater', :url => new_survey_section_question_path(@survey,@section, :insert_below_id => :xxx, :variation => :repeater), :method => :get